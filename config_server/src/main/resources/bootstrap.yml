server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/2014021008/springcloud_Demo/
          search-paths: config-repo
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:1110/eureka
  instance:
    prefer-ip-address: true

info:
  app:
    name: configServer
  company: www.xxx.com